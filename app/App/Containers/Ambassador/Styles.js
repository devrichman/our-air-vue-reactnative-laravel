import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  picture: {
    borderRadius: 5,
    height: 120,
    width: 120,
  },
  titleView: {
    marginTop: 30,
  },
})

export default styles
